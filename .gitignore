# ── Dependencias ─────────────────────────────────────────────────────────────
node_modules
frontend/node_modules
backend/node_modules

# ── Build del frontend ────────────────────────────────────────────────────────
frontend/dist
frontend/dist-ssr

# ── Credenciales — NUNCA subir al repositorio ─────────────────────────────────
.env
backend/.env
frontend/.env

# ── Datos locales — en producción viven en el servidor ───────────────────────
backend/data.json
backend/data.json.bak
backend/history.json

# ── Exports de Planner — datos internos, nunca subir al repositorio ───────────
planners/

# ── Logs ─────────────────────────────────────────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Copia local del propio repo (tiene su propio .git) — nunca versionar ─────
# Apareció al descomprimir un respaldo dentro del árbol de trabajo.
project_tracker_BD/

# ── Material de referencia externo, no forma parte de la app ─────────────────
Contro O.T/
Contro O.T.zip
pqrs-front-ref/

# ── Editor y herramientas locales ────────────────────────────────────────────
.claude/
markit/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.local
